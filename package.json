{
  "name": "devforge",
  "version": "0.1.0-beta",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "turbo": "^1.13.4",
    "typescript": "^5.0.0",
    "prettier": "^3.0.0",
    "husky": "^8.0.0",
    "eslint": "^8.0.0"
  }
}

